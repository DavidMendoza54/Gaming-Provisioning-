apiVersion: 1
prune: true

datasources:
  - name: Prometheus
    uid: tinyprovisioner-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 5s
